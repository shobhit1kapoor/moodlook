extends: base.yaml
model:
  name: masked_temporal_unet
  base_channels: 32
  dropout: 0.08
training:
  batch_size: 24
  epochs: 45
  early_stopping_patience: 10
  lr: 0.00025
  sensor_weighted_loss: true
  flip_augmentation: true
