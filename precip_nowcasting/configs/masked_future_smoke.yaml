extends: masked_temporal_future.yaml
model:
  base_channels: 8
  dropout: 0.0
training:
  epochs: 1
  batch_size: 4
  num_workers: 0
  early_stopping_patience: 1
  normalization_samples_per_sensor: 32
